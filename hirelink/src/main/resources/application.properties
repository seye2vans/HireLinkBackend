server.port=8081

# ? Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d3p7p7euk2gs73f0alf0-a/recommendation_db_zta1}
spring.datasource.username=${DATABASE_USERNAME:recommendation_db_zta1_user}
spring.datasource.password=${DATABASE_PASSWORD:IssucI4ETKSnAVaFheb49Bf6CeXlB1t3}
spring.datasource.driver-class-name=org.postgresql.Driver

# ? Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ? JWT Secret
app.jwt.secret=${JWT_SECRET:mysecretkey123}
